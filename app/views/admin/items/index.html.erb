<h1>商品一覧</h1>
<% @items.each do |item| %>
  <%= image_tag item.image, size: "50×50" %>
  <td><%= item.name %></td>
  <%= item.price %>
  <% if item.is_active == true %>
    <p>販売中</p>
  <% else %>
    <p>販売停止中</p>
  <% end %>
<% end %>